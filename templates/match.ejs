<div class="match-page-wrapper">
    <div class="cinematic-bg" style="background-image: url('<%= event.thumbnail %>')"></div>

    <section class="container match-main">
        <div class="match-hero">
            <div class="match-meta-top">
                <span class="sport-label">
                    <%= event.sport.toUpperCase() %>
                </span>
                <span class="league-label">
                    <%= event.league %>
                </span>
                <% if (event.status==='live' ) { %>
                    <span class="live-indicator" data-live="true">🔴 LIVE NOW</span>
                    <% } %>
            </div>

            <h1 class="match-title">
                <%= h1 %>
            </h1>

            <div class="match-time-info">
                <span class="time-badge">
                    ⏱️ <%= new Date(event.startTime).toLocaleString([], { weekday: 'short' , month: 'short' ,
                        day: 'numeric' , hour: '2-digit' , minute: '2-digit' }) %>
                </span>
            </div>
        </div>

        <!-- PAA Block (Above the Fold) -->
        <div class="paa-block-top">
            <div class="paa-card">
                <h3>How to watch <%= event.name %>?</h3>
                <p>You can watch <%= event.name %> live for free on Footybite. Simply use the player below to start the
                        high-definition stream.</p>
            </div>
            <div class="paa-card">
                <h3>Is it free?</h3>
                <p>Yes, Footybite (Fotybyte) provides 100% free access to this <%= event.league %> match without any
                        subscription required.</p>
            </div>
        </div>

        <div class="player-section">
            <div class="player-aspect-ratio">
                <div class="player-container">
                    <iframe src="<%= event.iframe %>" allowfullscreen scrolling="no"
                        allow="autoplay; encrypted-media"></iframe>
                </div>
            </div>
            <div class="player-controls-hint">
                <span>Tip: If the stream freezes, try refreshing the page. Last updated: <%= new
                        Date(lastUpdated).toLocaleTimeString() %></span>
            </div>
        </div>

        <div class="match-details-grid">
            <div class="match-info-card">
                <h2>Event Information</h2>
                <div class="info-row">
                    <span class="label">Match:</span>
                    <span class="value">
                        <%= event.name %>
                    </span>
                </div>
                <div class="info-row">
                    <span class="label">Competition:</span>
                    <span class="value">
                        <%= event.league %>
                    </span>
                </div>
                <div class="info-row">
                    <span class="label">Kickoff:</span>
                    <span class="value">
                        <%= new Date(event.startTime).toLocaleString() %>
                    </span>
                </div>
                <div class="info-row">
                    <span class="label">Status:</span>
                    <span class="value status-<%= event.status %>">
                        <%= event.status.toUpperCase() %>
                    </span>
                </div>
            </div>

            <div class="match-seo-card">
                <h2>Streaming Details</h2>
                <p>Experience <strong>
                        <%= event.name %>
                    </strong> live. Footybite is the most trusted source for <%= event.sport %> fans to find reliable
                        links.</p>
                <p>Our links are updated hourly to ensure zero downtime. Watch on any device: Mobile, PC, or Tablet.</p>
            </div>
        </div>

        <!-- Related Matches -->
        <% if (related && related.length> 0) { %>
            <div class="related-matches-section">
                <h2 class="section-title">Related Matches</h2>
                <div class="match-grid">
                    <% related.forEach(rel=> { %>
                        <a href="/<%= rel.url %>" class="match-card-mini">
                            <div class="mini-thumb" style="background-image: url('<%= rel.thumbnail %>')"></div>
                            <div class="mini-info">
                                <span class="mini-league">
                                    <%= rel.league %>
                                </span>
                                <span class="mini-name">
                                    <%= rel.name %>
                                </span>
                            </div>
                        </a>
                        <% }) %>
                </div>
            </div>
            <% } %>

                <div class="seo-content-bottom">
                    <h3>Frequently Asked Questions</h3>
                    <div class="faq-grid">
                        <div class="faq-item">
                            <h4>Where can I stream <%= event.name %>?</h4>
                            <p>Footybite (Footybyte) is the best place to stream <%= event.name %> live. We offer
                                    multiple mirror links to ensure you never miss the kickoff.</p>
                        </div>
                        <div class="faq-item">
                            <h4>What time does the match start?</h4>
                            <p>The match between <%= event.teams[0] %> and <%= event.teams[1] %> starts at <%= new
                                            Date(event.startTime).toLocaleTimeString() %> on <%= new
                                                Date(event.startTime).toLocaleDateString() %>.</p>
                        </div>
                    </div>
                </div>
    </section>
</div>