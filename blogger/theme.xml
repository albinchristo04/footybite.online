<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE html>
<html b:css='false' b:defaultwidgetversion='2' b:layoutsVersion='3' b:responsive='true' b:templateUrl='indie.xml' b:templateVersion='1.3.0' expr:dir='data:blog.languageDirection' expr:lang='data:blog.locale' xmlns='http://www.w3.org/1999/xhtml' xmlns:b='http://www.google.com/2005/gml/b' xmlns:data='http://www.google.com/2005/gml/data' xmlns:expr='http://www.google.com/2005/gml/expr'>
  <head>
    <meta content='width=device-width, initial-scale=1' name='viewport'/>
    <b:include name='all-head-content'/>
    <title><data:blog.pageTitle/></title>
    
    <b:skin><![CDATA[
/* ============================================
   FOOTYBITE BLOGGER THEME - DARK SPORTS UI
   ============================================ */

:root {
    --primary: #00d4ff;
    --accent: #ff6b35;
    --dark: #0a0e27;
    --darker: #060920;
    --light: #ffffff;
    --gray: #a0aec0;
    --glass: rgba(255, 255, 255, 0.05);
    --glass-border: rgba(255, 255, 255, 0.1);
    --amber: #fbbf24;
}

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
    background: linear-gradient(135deg, var(--darker) 0%, var(--dark) 100%);
    color: var(--light);
    line-height: 1.6;
    min-height: 100vh;
}

.container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 20px;
}

/* HEADER */
header {
    background: var(--glass);
    backdrop-filter: blur(10px);
    border-bottom: 1px solid var(--glass-border);
    padding: 20px 0;
    position: sticky;
    top: 0;
    z-index: 1000;
}

.nav-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.logo {
    font-size: 1.8rem;
    font-weight: 900;
    background: linear-gradient(135deg, var(--primary), var(--accent));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    text-decoration: none;
}

.nav-links {
    display: flex;
    gap: 30px;
}

.nav-links a {
    color: var(--gray);
    text-decoration: none;
    font-weight: 600;
    transition: color 0.3s;
}

.nav-links a:hover {
    color: var(--primary);
}

/* MATCH CARD */
.match-card {
    background: var(--glass);
    border: 1px solid var(--glass-border);
    border-radius: 16px;
    overflow: hidden;
    transition: all 0.3s ease;
    text-decoration: none;
    display: block;
    margin-bottom: 20px;
}

.match-card:hover {
    transform: translateY(-5px);
    border-color: var(--primary);
    box-shadow: 0 10px 30px rgba(0, 212, 255, 0.2);
}

.match-thumb-wrapper {
    position: relative;
    padding-top: 56.25%;
    background: var(--darker);
    overflow: hidden;
}

.match-thumb-wrapper img {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
}

.league-badge {
    position: absolute;
    top: 10px;
    left: 10px;
    background: rgba(0, 0, 0, 0.8);
    backdrop-filter: blur(10px);
    padding: 5px 12px;
    border-radius: 20px;
    font-size: 0.75rem;
    font-weight: 700;
    text-transform: uppercase;
    color: var(--primary);
    z-index: 2;
}

.match-card-content {
    padding: 20px;
}

.match-date-small {
    font-size: 0.85rem;
    color: var(--gray);
    margin-bottom: 10px;
}

.match-teams {
    margin: 15px 0;
}

.team-row {
    padding: 8px 0;
    font-size: 1.1rem;
    font-weight: 600;
}

.countdown-text {
    display: inline-block;
    padding: 8px 16px;
    background: rgba(251, 191, 36, 0.1);
    border: 1px solid var(--amber);
    border-radius: 8px;
    color: var(--amber);
    font-weight: 700;
    font-size: 0.9rem;
}

.countdown-text.live {
    background: rgba(239, 68, 68, 0.1);
    border-color: #ef4444;
    color: #ef4444;
}

/* PLAYER SECTION */
.player-section {
    margin: 40px 0;
}

.player-aspect-ratio {
    position: relative;
    padding-top: 56.25%;
    background: var(--darker);
    border-radius: 16px;
    overflow: hidden;
}

.player-container {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
}

.player-container iframe {
    width: 100%;
    height: 100%;
    border: none;
}

.countdown-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.9);
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    z-index: 10;
}

.countdown-overlay h2 {
    color: var(--primary);
    font-size: 2rem;
    margin-bottom: 20px;
}

#countdown-timer {
    font-size: 3rem;
    font-weight: 900;
    color: var(--amber);
    font-family: 'Courier New', monospace;
}

/* MATCH DETAILS */
.match-details-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 20px;
    margin: 40px 0;
}

.match-info-card,
.match-seo-card {
    background: var(--glass);
    padding: 30px;
    border-radius: 16px;
    border: 1px solid var(--glass-border);
}

.match-info-card h2,
.match-seo-card h2 {
    margin-bottom: 20px;
    color: var(--primary);
}

.info-row {
    display: flex;
    justify-content: space-between;
    padding: 12px 0;
    border-bottom: 1px solid var(--glass-border);
}

.info-row .label {
    color: var(--gray);
    font-weight: 600;
}

.info-row .value {
    color: var(--light);
    font-weight: 700;
}

.news-list {
    list-style: none;
}

.news-list li {
    margin-bottom: 12px;
    padding-left: 20px;
    position: relative;
    color: var(--gray);
}

.news-list li::before {
    content: '•';
    position: absolute;
    left: 0;
    color: var(--primary);
    font-weight: 900;
}

/* FAQ */
.faq-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 20px;
    margin: 40px 0;
}

.faq-item {
    background: var(--glass);
    padding: 20px;
    border-radius: 12px;
    border: 1px solid var(--glass-border);
}

.faq-item h4 {
    color: var(--primary);
    margin-bottom: 10px;
}

.faq-item p {
    color: var(--gray);
    font-size: 0.9rem;
}

/* FOOTER */
footer {
    background: var(--glass);
    border-top: 1px solid var(--glass-border);
    padding: 40px 0;
    margin-top: 80px;
    text-align: center;
    color: var(--gray);
}

/* RESPONSIVE */
@media (max-width: 768px) {
    .nav-links {
        display: none;
    }
    
    .match-details-grid,
    .faq-grid {
        grid-template-columns: 1fr;
    }
    
    #countdown-timer {
        font-size: 2rem;
    }
}

/* POST BODY */
.post-body {
    max-width: 900px;
    margin: 0 auto;
    padding: 40px 20px;
}

.post-body h1 {
    font-size: 2.5rem;
    margin-bottom: 20px;
    background: linear-gradient(135deg, var(--primary), var(--accent));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
}

.post-body h2 {
    color: var(--primary);
    margin: 30px 0 15px;
}

.post-body p {
    color: var(--gray);
    margin-bottom: 15px;
    line-height: 1.8;
}

/* HIDE BLOGGER DEFAULTS */
.blog-pager,
.post-footer,
.post-timestamp,
.post-author,
.post-comment-link {
    display: none !important;
}
    ]]></b:skin>
    
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;900&amp;display=swap" rel="stylesheet"/>
  </head>
  
  <body>
    <!-- HEADER -->
    <b:section class='header' id='header' maxwidgets='1' showaddelement='no'>
      <b:widget id='Header1' locked='true' title='FootyBite (Header)' type='Header' version='2' visible='true'>
        <b:includable id='main'>
          <header>
            <div class='container nav-content'>
              <a class='logo' expr:href='data:blog.homepageUrl'>FOOTYBITE</a>
              <nav class='nav-links'>
                <a href='/search/label/Football'>Football</a>
                <a href='/search/label/NFL'>NFL</a>
                <a href='/search/label/NBA'>NBA</a>
                <a href='/search/label/Boxing'>Boxing</a>
                <a href='/search/label/Live'>All Streams</a>
              </nav>
            </div>
          </header>
        </b:includable>
      </b:widget>
    </b:section>
    
    <!-- MAIN CONTENT -->
    <b:section class='main' id='main' showaddelement='no'>
      <b:widget id='Blog1' locked='true' title='Blog Posts' type='Blog' version='2' visible='true'>
        <b:includable id='main' var='top'>
          <div class='container'>
            <b:if cond='data:view.isMultipleItems'>
              <!-- HOMEPAGE / CATEGORY VIEW -->
              <b:loop values='data:posts' var='post'>
                <data:post.body/>
              </b:loop>
            <b:else/>
              <!-- SINGLE POST VIEW -->
              <b:loop values='data:posts' var='post'>
                <div class='post-body'>
                  <data:post.body/>
                </div>
              </b:loop>
            </b:if>
          </div>
        </b:includable>
      </b:widget>
    </b:section>
    
    <!-- FOOTER -->
    <b:section class='footer' id='footer' maxwidgets='1' showaddelement='no'>
      <b:widget id='HTML1' locked='false' title='' type='HTML' version='2' visible='true'>
        <b:includable id='main'>
          <footer>
            <div class='container'>
              <p>© 2025 FootyBite. All rights reserved. Watch live sports streams for free.</p>
            </div>
          </footer>
        </b:includable>
      </b:widget>
    </b:section>
    
    <!-- COUNTDOWN SCRIPT -->
    <script>
    //<![CDATA[
    function updateCountdown() {
        const timerEl = document.getElementById('countdown-timer');
        if (!timerEl) return;
        
        const startTime = parseInt(timerEl.getAttribute('data-start'));
        const now = Date.now();
        const diff = startTime - now;
        
        if (diff <= 0) {
            timerEl.innerHTML = '🔴 LIVE NOW';
            const overlay = document.querySelector('.countdown-overlay');
            if (overlay && diff > -1800000) {
                window.location.reload();
            }
            return;
        }
        
        const hours = Math.floor(diff / (1000 * 60 * 60));
        const minutes = Math.floor((diff % (1000 * 60 * 60)) / (1000 * 60));
        const seconds = Math.floor((diff % (1000 * 60)) / 1000);
        
        timerEl.innerHTML = `⏱️ Starts in ${hours.toString().padStart(2, '0')}h ${minutes.toString().padStart(2, '0')}m ${seconds.toString().padStart(2, '0')}s`;
        
        if (diff <= 30 * 60 * 1000) {
            window.location.reload();
        }
    }
    
    if (document.getElementById('countdown-timer')) {
        setInterval(updateCountdown, 1000);
        updateCountdown();
    }
    //]]>
    </script>
  </body>
</html>
